kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: hello-app-pvc
  namespace: kube-kubedemo
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: gp2
